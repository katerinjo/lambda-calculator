import React from "react";

export default NumberButton = ({ children }) => <button>{children}</button>;
/* Display a button element rendering the data being passed down from the parent container on props */